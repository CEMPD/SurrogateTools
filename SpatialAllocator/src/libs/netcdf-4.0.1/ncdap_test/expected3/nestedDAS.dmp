netcdf nestedDAS {
dimensions:
	unlimited = UNLIMITED ; // (0 currently)
	stringdim64 = 64 ;
variables:
	byte b31 ;
	char b32(stringdim64) ;
data:

 b31 = 0 ;

 b32 = "This is a data test string (pass 0)." ;
}
